services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_PASSWORD: replaylab
      POSTGRES_USER: replaylab
      POSTGRES_DB: replaylab
    ports: ["5432:5432"]
    volumes: ["./postgres-init.sql:/docker-entrypoint-initdb.d/init.sql"]
