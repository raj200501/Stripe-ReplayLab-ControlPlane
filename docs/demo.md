# Demo

Run:

```bash
make demo
```

This command seeds scenarios, starts merchant + API services, executes a deterministic run, and prints dashboard startup command.

Additional operations:
- `make seed`
- `make replay RUN_ID=<id>`
- `make reset`
